# yaml-language-server: $schema=../../../LeMoS-Szenario-Schema.json
scenario:
  id: S2
  explanation: "form input[type='checkbox']:checked + label"
  initialization:
    - S.setValue("form input[type= 'checkbox']:checked + label")